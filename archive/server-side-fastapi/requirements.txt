# LocInsight ML Engine — Python dependencies
# Pinned for HF Spaces Gradio SDK (free tier, August 2026)

# Web framework + Gradio UI (HF Spaces Gradio SDK requires gradio)
gradio==4.44.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# HTTP client (for Supabase REST + Overpass API)
httpx==0.28.1

# ML / Data science
numpy==2.2.1
scikit-learn==1.6.0
scipy==1.14.1

# Utilities
python-dotenv==1.0.1
